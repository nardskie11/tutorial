//CommonJS, every file is module (by default)
//Modules - Encapsulated code(only share minimun)
const names = require('./4-name')
const sayHi = require('./5-util')
const data = require('./6-alternative-flavor')

// require('./7-basic-math')
// sayHi(names.kulot)
// sayHi(names.nard).

console.log(data)